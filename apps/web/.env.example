VITE_API_URL=http://localhost:3000
# "dev" = type any email to log in (local only). "cognito" = real login.
VITE_AUTH_MODE=dev
# Only needed when VITE_AUTH_MODE=cognito (values come from `terraform output`)
VITE_COGNITO_AUTHORITY=
VITE_COGNITO_CLIENT_ID=
VITE_COGNITO_DOMAIN=
