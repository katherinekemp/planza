DATABASE_URL=postgres://planza:planza@localhost:5432/planza
PORT=3000
# Comma-separated list of origins allowed to call the API from a browser
CORS_ORIGINS=http://localhost:5173
# "dev" lets you log in by typing an email (local only). "cognito" verifies real tokens.
AUTH_MODE=dev
